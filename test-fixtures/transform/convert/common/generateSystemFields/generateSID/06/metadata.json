{
  "description": "Generates fSID using both dc.identifier.uri and dc.source.identifier values when base URLs are found from list of valid sources",
  "source": "foobar.example.com",
  "only": false
}